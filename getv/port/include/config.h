/* GoldenEye tvOS port — Fast3D pulls in the game's config.h for compile-time
 * feature switches. GoldenEye has no equivalent, so the defaults apply. */
#ifndef GE_PORT_CONFIG_H
#define GE_PORT_CONFIG_H
#endif
