/* GoldenEye tvOS port — sm64ex's Fast3D SDL backend includes this for command-line
 * options. tvOS apps have no command line, and the backend references none of the
 * fields, so this is deliberately empty rather than a fake options struct. */
#ifndef GE_PORT_CLIOPTS_H
#define GE_PORT_CLIOPTS_H
#endif
