--- a/src/boss.c
+++ b/src/boss.c
@@ -199,6 +199,7 @@
     gePortBootMark("rspInit"); rspInit();
     gePortBootMark("dynInit"); dynInit();
     gePortBootMark("joyInit"); joyInit();
+    gePortBootMark("gePortNetInit"); gePortNetInit();
     osCreateMesgQueue(&bossmq, &bossmsg, 1);
 
     for (i = 0; i != MAXCONTROLLERS; i++)
@@ -595,6 +596,21 @@
                             gePortBootMark("bm:permit_stderr");
                             permit_stderr(0);
 
+                            // Netplay: stall gates the whole tick/render pass below, not just
+                            // an input queue, because the render loop and the world-simulation
+                            // tick are one interleaved loop here (lvlRender calls into it) --
+                            // see docs/NETPLAY.md. Read after joyConsumeSamplesWrapper() above
+                            // so gePortNetTick() captures this tick's real input, not the
+                            // previous one's. Same "nothing to do, come back around" shape as
+                            // the mainTickElapsed check above: a stalled machine that ran ahead
+                            // of input it does not have yet is a machine about to desync.
+                            if (!gePortNetTick())
+                            {
+                                // nothing to do -- waiting on a peer for this tick.
+                            }
+                            else
+                            {
+
                             gdl = firstGdl = dynGetMasterDisplayList();
 
 #ifdef DEBUGMENU
@@ -750,6 +766,8 @@
 
                             speedgraphMarkerHandler(0x10000);
                             if(1);
+
+                            } // netplay: else for gePortNetTick() above
                         }
                     }
                 }
--- a/src/ge_port_decls.h
+++ b/src/ge_port_decls.h
@@ -1,5 +1,7 @@
 extern void gePortBootMark(const char *what);   /* port boot tracing; see port_support.c */
 extern void gePortCheckItemEntries(const char *where); /* c_item_entries corruption canary */
+extern int gePortNetInit(void);   /* netplay boot: opens a socket if GETV_NET_HOST/JOIN is set; see ge_net_udp.c */
+extern int gePortNetTick(void);   /* netplay per-tick: 1 to proceed, 0 to stall; see ge_net_udp.c */
 /* GENERATED by tools/gen_port_decls.py - do not edit by hand.
  *
  * IDO accepted implicit function declarations; C99+/clang does not. Every
